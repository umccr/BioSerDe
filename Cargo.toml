[package]
name = "bioserde"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1"
prost = "0.10.1"
prost-types = "0.10.1"
thiserror = "1.0.31"

noodles = { version = "0.24.0", features = ["bed", "fastq","core"] }

[build-dependencies]
prost-build = "0.10.1"

[dev-dependencies]
criterion = "0.3.5"

[[bench]]
name = "example"
harness = false